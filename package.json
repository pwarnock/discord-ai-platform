{
  "name": "n8n-discord-integration",
  "version": "1.0.0",
  "private": true,
  "packageManager": "bun@1.3.3",
  "workspaces": [
    "packages/n8n-workflows"
  ],
  "scripts": {
    "build": "turbo build",
    "test": "turbo test",
    "deploy": "turbo deploy",
    "dev": "turbo dev",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "turbo": "^2.6.1"
  },
  "lint-staged": {
    "*.{ts,js,json}": [
      "bash -c 'turbo test'"
    ],
    ".env": [
      "bash -c 'echo \"❌ Error: .env files should not be committed!\" && exit 1'"
    ]
  }
}
